# Examples

The output of every example script can be found in the reference_exports directory.

When you have the script slice_matrix_basic.py, the associated export is named slice_matrix_basic.png in the reference_exports directory.

Please note, that all example scripts were executed to generate the reference exports with certain OpenFoam datasets, which cannot be included in this repository.