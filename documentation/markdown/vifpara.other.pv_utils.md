# PV Utils

### *class* PaletteOption(\*values)

Bases: [`IntEnum`](https://docs.python.org/3/library/enum.html#enum.IntEnum)

### set_palette(option=PaletteOption.WHITE)

Set the ParaView rendering palette based on a palette option.

* **Parameters:**
  **option** ([*PaletteOption*](#vifpara.other.pv_utils.PaletteOption)) – The palette mode to apply, as defined in the
  `PaletteOption` enum.
* **Returns:**
  None
